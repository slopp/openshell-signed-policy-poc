# Example policy

This example policy models a signed deployment for the `nemoclaw-community` subject and the `personal-community-sentiment-triage` policy name. The policy stays intentionally small but includes enough structure to demonstrate a plausible moderation and routing configuration.
